{
  "name": "@shadcn-calendar/tooling",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit --pretty false",
    "lint": "tsc -p tsconfig.json --noEmit --pretty false",
    "typecheck": "tsc -p tsconfig.json --noEmit --pretty false",
    "test": "echo 'No tooling tests yet'",
    "release:pack-check": "node src/release-pack-check.mjs"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}

{
  "name": "calendar-kit-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Open-source calendar kit workspace",
  "packageManager": "pnpm@10.17.1",
  "scripts": {
    "dev": "pnpm --filter @calendar-kit/docs dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "release:check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build",
    "release:pack": "node packages/tooling/src/release-pack-check.mjs",
    "release:publish:next": "pnpm --filter @calendar-kit/core publish --tag next --access public && pnpm --filter @calendar-kit/registry publish --tag next --access public"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
